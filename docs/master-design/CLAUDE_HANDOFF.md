# Claude Handoff — Home Master Design

Issue #42 is the implementation task.

## Current Master Design
The Project Owner's latest uploaded screenshot in the ChatGPT task is the authoritative visual source. The repository text reference is only a backup description.

## IMPORTANT — binary asset handoff
A local reference pack has been prepared from the Owner's screenshot, including the full-page 2x reference and section crops. The current GitHub connector can write UTF-8 text files but cannot upload binary PNG/ZIP files into the repository or attach them to an Issue. Therefore **do not claim the image pack is available in GitHub yet**.

Until the binary references are actually accessible in the execution environment:
- Do not start a visual redesign from memory.
- Do not invent replacement artwork/icons/backgrounds.
- Do not claim pixel-level visual comparison against the screenshot.
- Use `home-page-master-design.md` only as the textual fallback.

## Planned implementation method
When the binary references become accessible, implement the Home in controlled sections, starting with Hero:
1. Header
2. Hero
3. Category icons
4. Stats
5. Recommended contractors
6. How to use
7. Why use
8. Contractor CTA
9. Testimonials
10. Articles
11. Footer

For each section, use the corresponding supplied image reference as source of truth. Preserve composition, proportions, typography hierarchy, spacing, imagery, colors and visual density. Do not redraw an element when an exact supplied asset is available.

## Locked contractor benefits
1. `เพิ่มโปรไฟล์ฟรี`
2. `ลงผลงานฟรี`
3. `เข้าถึงลูกค้าทั่วไทย`
4. `สร้างความน่าเชื่อถือ`

## Workflow
Do not begin another implementation pass merely because this handoff document exists. Wait for the Project Owner's explicit instruction through Issue #42 after the visual reference assets are accessible.
